ClientAuthenticationPacket = module.exports = function() {
};

ClientAuthenticationPacket.parse = function(data) {
  var packet = new ClientAuthenticationPacket();
  // TODO: If needed

  return packet;
};
